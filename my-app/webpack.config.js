const path = require('path');

module.exports = {
  // other configurations...

  devServer: {
    allowedHosts: 'all', // Change this to a specific host if needed
  },

  // other configurations...
};
